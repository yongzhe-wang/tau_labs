---
import PageLayout from "@/layouts/Base.astro";
---

<PageLayout meta={{ title: "Solutions" }}>
	<section class="mx-auto max-w-7xl px-4 py-20 sm:px-8">
		<h1 class="mb-6 text-4xl font-bold text-text-primary">Production-Ready Modules & Systems</h1>
		<p class="mb-12 text-xl text-text-secondary leading-relaxed max-w-prose">
			From silicon to shelf. We provide the validated modules, platforms, and manufacturing execution to ship intelligent products.
		</p>

		<div class="grid grid-cols-1 gap-12">
			{[
				{
					title: "Conversational Device Platform",
					whatItIs: "A production-ready voice system for toys and devices.",
					sections: [
						{ label: "Includes", items: ["Wake word + interrupt (local)", "Hybrid cloud dialogue", "OTA + device registry", "Companion API"] },
						{ label: "Typical Use", items: ["Toys", "Desktop robots", "Voice accessories"] }
					],
					highlight: "Time to Pilot: 30 days"
				},
				{
					title: "Custom AI Compute Modules",
					whatItIs: "Pre-validated compute and IO modules tailored to your form factor.",
					sections: [
						{ label: "Deliverables", items: ["Carrier board", "Power + thermal design", "Manufacturing files", "Test firmware"] }
					],
					whyItMatters: "Reduces board spins and factory failures."
				},
				{
					title: "Manufacturing & Scale Execution",
					whatItIs: "We bridge the gap between prototype and mass production, managing the factory floor to ensure quality at scale.",
					sections: [
						{ label: "Capabilities", items: ["Pilot run support", "Flashing + QA workflow", "Yield tracking", "CM handoff"] }
					],
					whyItMatters: "We understand factories, ensuring high yield and reliable delivery."
				},
			].map((item) => (
				<div class="border-gray-800 rounded-xl border bg-gray-900 p-8">
					<div class="flex flex-col md:flex-row md:items-start md:justify-between gap-4 mb-6">
						<div>
							<h2 class="text-2xl font-bold text-text-primary">{item.title}</h2>
							<p class="mt-2 text-lg text-text-secondary leading-relaxed">{item.whatItIs}</p>
						</div>
						{item.highlight && (
							<div class="bg-accent/10 text-accent px-4 py-2 rounded-full text-sm font-bold whitespace-nowrap border border-accent/20">
								{item.highlight}
							</div>
						)}
					</div>

					<div class="grid grid-cols-1 md:grid-cols-2 gap-8">
						{item.sections.map((section) => (
							<div>
								<h3 class="mb-3 font-bold text-accent uppercase tracking-wider text-sm">{section.label}</h3>
								<ul class="space-y-2">
									{section.items.map((d) => (
										<li class="flex items-start text-text-tertiary">
											<span class="mr-2 text-accent">•</span>
											{d}
										</li>
									))}
								</ul>
							</div>
						))}
						
						{item.whyItMatters && (
							<div class="md:col-span-2 bg-gray-800/50 rounded-lg p-4 mt-2 border border-gray-700/50">
								<span class="text-accent font-bold mr-2">Why it matters:</span>
								<span class="text-gray-300">{item.whyItMatters}</span>
							</div>
						)}
					</div>
				</div>
			))}
		</div>

		<div class="mt-20 border-t border-gray-800 pt-20">
			<h2 class="mb-12 text-3xl font-bold">Frequently Asked Questions</h2>
			<div class="space-y-8">
				<div>
					<h3 class="mb-2 text-xl font-bold">Minimum engagement size?</h3>
					<p class="text-gray-400">We typically work with teams ready to prototype or scale. Contact us to discuss your specific needs.</p>
				</div>
				<div>
					<h3 class="mb-2 text-xl font-bold">Lead time?</h3>
					<p class="text-gray-400">Standard design cycles range from 4-12 weeks depending on complexity.</p>
				</div>
				<div>
					<h3 class="mb-2 text-xl font-bold">Do you handle manufacturing?</h3>
					<p class="text-gray-400">We handle prototyping and pilot runs, and partner with trusted CMs for mass production.</p>
				</div>
			</div>
		</div>
	</section>
</PageLayout>